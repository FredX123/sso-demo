server:
  port: 6004

spring:
  config:
    activate:
      on-profile: default

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://integrator-5973406.okta.com/oauth2/default

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost

mock:
  permissions:
    - app: backoffice
      rules:
        - action: GET
          resource: "/api/backoffice/**"
          roles: [ADMIN]
        - action: POST
          resource: "/api/backoffice/**"
          roles: [ ADMIN ]
        - action: GET
          resource: "/api/backoffice/whoami-from-fo"
          roles: [ADMIN]
        - action: GET
          resource: "/api/backoffice/whoami"
          roles:
            - USER
            - ADMIN

    - app: frontoffice
      rules:
        - action: GET
          resource: "/api/frontoffice/**"
          roles: [USER]
        - action: POST
          resource: "/api/frontoffice/**"
          roles: [USER]